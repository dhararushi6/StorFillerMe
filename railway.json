{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter ./packages/** run build && pnpm --filter @storefiller/api exec prisma generate && pnpm --filter @storefiller/api run build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @storefiller/api exec prisma migrate deploy && node apps/api/dist/server.js",
    "numReplicas": 1,
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
